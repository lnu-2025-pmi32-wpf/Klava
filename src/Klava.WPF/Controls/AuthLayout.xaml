<UserControl x:Class="Klava.WPF.Controls.AuthLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:controls="clr-namespace:Klava.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             x:Name="Root">

    <Grid>
        <controls:LoadingOverlay IsVisible="{Binding IsLoading, ElementName=Root}" Panel.ZIndex="10"/>

        <Border MaxWidth="480" 
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="40">
            
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.15" Color="Black"/>
            </Border.Effect>

            <StackPanel>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,0,0,25">
                    <TextBlock Text="{Binding Title, ElementName=Root}" 
                               FontSize="26" FontWeight="Bold" 
                               Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Subtitle, ElementName=Root}" 
                               FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" 
                               HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>

                <ContentPresenter Content="{Binding CardContent, ElementName=Root}" />
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>