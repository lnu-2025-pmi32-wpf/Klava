<UserControl x:Class="Klava.WPF.Views.SubjectDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Klava.WPF.Converters"
             xmlns:controls="clr-namespace:Klava.WPF.Controls"
             MinWidth="760" MinHeight="480"
             Background="{StaticResource BackgroundBrush}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <converters:StatusToBrushConverter x:Key="StatusToBrushConverter"/>
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="‹" 
                        Command="{Binding GoBackCommand}"
                        Padding="6,4"
                        Style="{StaticResource GhostButton}"
                        Foreground="{StaticResource PrimaryBrush}"
                        Margin="0,0,12,0"
                        FontSize="22"
                        FontWeight="Bold"
                        ToolTip="Повернутись до предметів"/>
                        
                <TextBlock Text="{Binding Subject.Title}" 
                           FontSize="28" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
                           
                <Border Background="{Binding Subject.Status, Converter={StaticResource StatusToBrushConverter}}"
                        CornerRadius="12"
                        Padding="8,4"
                        Margin="15,0,0,0"
                        VerticalAlignment="Center">
                    <TextBlock Text="{Binding Subject.Status}" 
                               Foreground="White"
                               FontSize="12"
                               FontWeight="Bold"/>
                </Border>
            </StackPanel>

            <TextBlock Text="{Binding Subject.SubjectInfo}" 
                       FontSize="14"
                       Foreground="#666"
                       TextWrapping="Wrap"
                       Visibility="{Binding Subject.SubjectInfo, Converter={StaticResource NullToVisibility}}"/>

            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="14"
                       Foreground="#4CAF50"
                       Margin="0,10,0,0"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}"/>
        </StackPanel>

        <!-- Files Section -->
        <Border Grid.Row="1" 
                Background="White"
                BorderBrush="#e0e0e0"
                BorderThickness="1"
                CornerRadius="4"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Files Header -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="{Binding Files.Count, StringFormat='Файли ({{0}})'}" 
                               FontSize="20"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                               
                        <Button Content="Завантажити файли" 
                            Command="{Binding UploadFileCommand}"
                            Padding="15,10"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="14"
                            HorizontalAlignment="Right"
                            Cursor="Hand"
                            Visibility="{Binding IsHeadman, Converter={StaticResource BoolToVisibility}}"/>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Files}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#f9f9f9"
                                        BorderBrush="#e0e0e0"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="15"
                                        Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding DisplayName}" 
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Margin="0,0,0,5"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Size: " 
                                                           FontSize="12"
                                                           Foreground="#666"/>
                                                <TextBlock FontSize="12"
                                                           Foreground="#666">
                                                    <Run Text="{Binding Size, Mode=OneWay}"/>
                                                    <Run Text=" байт"/>
                                                </TextBlock>
                                                <TextBlock Text=" | " 
                                                           FontSize="12"
                                                           Foreground="#666"
                                                           Margin="5,0"/>
                                                <TextBlock Text="Завантажено: " 
                                                           FontSize="12"
                                                           Foreground="#666"/>
                                                <TextBlock Text="{Binding UploadedAt, StringFormat='MMM dd, yyyy HH:mm'}" 
                                                           FontSize="12"
                                                           Foreground="#666"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" 
                                                    Orientation="Horizontal"
                                                    VerticalAlignment="Center">
                                                <Button Content="Завантажити" 
                                                    Command="{Binding DataContext.DownloadFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="10,6"
                                                    Background="#2196F3"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Margin="0,0,10,0"
                                                    Cursor="Hand"
                                                    Style="{StaticResource ModernButton}"/>
                                            
                                                <Button Content="Видалити" 
                                                    Command="{Binding DataContext.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"
                                                    Padding="10,6"
                                                    Background="#F44336"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    Visibility="{Binding DataContext.IsHeadman, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibility}}"
                                                    Style="{StaticResource ModernButton}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Border Grid.Row="1" 
                        Background="#f9f9f9"
                        BorderBrush="#e0e0e0"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="40"
                        Visibility="{Binding Files.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- empty state icon removed for cleaner look -->
                        <TextBlock Text="Файли ще не завантажені" 
                                   FontSize="18"
                                   Foreground="#666"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Завантажте файли, щоб поділитися ними з командою" 
                                   FontSize="14"
                                   Foreground="#999"
                                   HorizontalAlignment="Center"
                                   Margin="0,5,0,0"
                                   Visibility="{Binding IsHeadman, Converter={StaticResource BoolToVisibility}}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <controls:LoadingOverlay Grid.Row="0" Grid.RowSpan="2" IsVisible="{Binding IsLoading}" />
    </Grid>
</UserControl>
